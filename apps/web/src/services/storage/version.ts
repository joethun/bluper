/**
 * Every project is stamped with this when it is saved. There is only ever
 * one storage version: Bluper has no users, so the migration history that
 * Opencut carried across the fork is gone.
 */
export const CURRENT_PROJECT_VERSION = 1;
